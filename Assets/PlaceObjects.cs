using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;
public class PlaceObjects : MonoBehaviourPunCallbacks
{
    // Start is called before the first frame update
    void Start()
    {
        //if Scene 1 etc

        //place the trees
        
        //place the poles

        //place the others
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
